# ── Fraud Detection — API requirements ────────────────────────────────────────
# Minimal set for running the scoring server only (no training dependencies).
# For training, also install requirements.txt.

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0

# ML inference (must match training versions for model compatibility)
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
lightgbm>=4.0

# HTTP client (load_test.py)
httpx>=0.27.0

# Config
pyyaml>=6.0
